spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/OpenRental_2
    password: postgres
    username: postgres
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true