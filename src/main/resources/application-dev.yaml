spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/OpenRental_3
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
debug: true
logging:
  level:
    org.flywaydb: DEBUG