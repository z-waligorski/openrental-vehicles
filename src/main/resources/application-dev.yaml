spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/openrental_vehicles
    username: postgres
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
debug: true
logging:
  level:
    org.flywaydb: DEBUG